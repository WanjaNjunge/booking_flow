// Placeholder — implementation in Session 2
export default function PostcodeLookup() {
  return (
    <div data-testid="postcode-lookup">
      <p>Step 1: Postcode Lookup — placeholder</p>
    </div>
  );
}
